# Pages

Vinext reserves `src/pages` for its legacy file router. Product page components
live in `src/screens` and are mapped through React Router in
`src/app/ClientApplication.tsx`; the `app/` directory only supplies the hosting
entrypoints and metadata.
