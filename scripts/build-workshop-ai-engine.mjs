import { createRequire } from 'node:module';
import { fileURLToPath } from 'node:url';
const require = createRequire(new URL('../frontend/package.json', import.meta.url));
const { build } = require('esbuild');
await build({ entryPoints: [fileURLToPath(new URL('../frontend/src/features/diet-workshop/aiEngine.ts', import.meta.url))],
  outfile: fileURLToPath(new URL('../supabase/functions/ai/workshop-engine.js', import.meta.url)),
  bundle: true, format: 'esm', platform: 'neutral', target: 'es2022',
  alias: { '@/src': fileURLToPath(new URL('../frontend/src', import.meta.url)) },
  banner: { js: '// GENERATED by scripts/build-workshop-ai-engine.mjs. Shared existing Workshop engines; do not edit.' } });
