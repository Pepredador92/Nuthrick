// GENERATED by scripts/build-diet-generation-domain.mjs. Offline domain only; no handler/provider integration.

// frontend/src/features/energy/catalog.ts
var MJ_TO_KCAL = 239.005736;
var references = {
  mifflin: {
    authors: "Mifflin MD, St Jeor ST, Hill LA, Scott BJ, Daugherty SA, Koh YO",
    year: 1990,
    title: "A new predictive equation for resting energy expenditure in healthy individuals",
    source: "American Journal of Clinical Nutrition, 51(2), 241\u2013247",
    pmid: "2305711",
    doi: "10.1093/ajcn/51.2.241",
    url: "https://pubmed.ncbi.nlm.nih.gov/2305711/"
  },
  harrisOriginal: {
    authors: "Harris JA, Benedict FG",
    year: 1919,
    title: "A Biometric Study of Human Basal Metabolism",
    source: "Carnegie Institution of Washington, Publication No. 279",
    url: "https://archive.org/details/biometricstudyof00harruoft"
  },
  rozaShizgal: {
    authors: "Roza AM, Shizgal HM",
    year: 1984,
    title: "The Harris Benedict equation reevaluated: resting energy requirements and the body cell mass",
    source: "American Journal of Clinical Nutrition, 40(1), 168\u2013182",
    pmid: "6741850",
    doi: "10.1093/ajcn/40.1.168",
    url: "https://pubmed.ncbi.nlm.nih.gov/6741850/"
  },
  valencia: {
    authors: "Valencia ME, Moya SY, McNeill G, Haggarty P",
    year: 1994,
    title: "Basal metabolic rate and body fatness of adult men in northern Mexico",
    source: "European Journal of Clinical Nutrition, 48(3), 205\u2013211",
    pmid: "8194506",
    url: "https://pubmed.ncbi.nlm.nih.gov/8194506/"
  },
  schofield: {
    authors: "Schofield WN",
    year: 1985,
    title: "Predicting basal metabolic rate, new standards and review of previous work",
    source: "Human Nutrition: Clinical Nutrition, 39 Suppl 1, 5\u201341",
    pmid: "4044297",
    url: "https://pubmed.ncbi.nlm.nih.gov/4044297/"
  },
  fao: {
    authors: "FAO, WHO, UNU",
    year: 2004,
    title: "Human energy requirements: Report of a Joint FAO/WHO/UNU Expert Consultation",
    source: "FAO Food and Nutrition Technical Report Series No. 1",
    url: "https://www.fao.org/4/y5686e/y5686e00.htm"
  }
};
var commonInputs = ["weightKg", "heightCm", "ageYears", "sex"];
var energyMethodCatalog = [
  {
    code: "MIFFLIN_ST_JEOR_1990",
    name: "Mifflin\u2013St Jeor 1990",
    shortName: "Mifflin\u2013St Jeor",
    author: "Mifflin et al.",
    year: 1990,
    catalogVersion: 1,
    methodVersion: "1.0.0",
    kind: "predictive_equation",
    resultType: "REE",
    resultUnit: "kcal/day",
    requiredInputs: [...commonInputs],
    applicability: {
      population: "Adultos de la muestra original, con y sin obesidad.",
      minAge: 19,
      maxAge: 78,
      sexScope: "male_and_female"
    },
    variants: [
      {
        code: "male",
        label: "Hombre",
        when: { sex: "male" },
        equation: {
          kind: "linear",
          intercept: 5,
          coefficients: { weightKg: 10, heightCm: 6.25, ageYears: -5 },
          sourceUnit: "kcal/day",
          expression: "10 \xD7 peso_kg + 6.25 \xD7 talla_cm \u2212 5 \xD7 edad_a\xF1os + 5"
        }
      },
      {
        code: "female",
        label: "Mujer",
        when: { sex: "female" },
        equation: {
          kind: "linear",
          intercept: -161,
          coefficients: { weightKg: 10, heightCm: 6.25, ageYears: -5 },
          sourceUnit: "kcal/day",
          expression: "10 \xD7 peso_kg + 6.25 \xD7 talla_cm \u2212 5 \xD7 edad_a\xF1os \u2212 161"
        }
      }
    ],
    references: [references.mifflin],
    notes: ["La edad fuera de 19\u201378 a\xF1os produce advertencia, no bloqueo autom\xE1tico."],
    warnings: ["Es una estimaci\xF3n predictiva y no sustituye calorimetr\xEDa indirecta."],
    active: true
  },
  {
    code: "HARRIS_BENEDICT_ORIGINAL_1919",
    name: "Harris\u2013Benedict original 1919",
    shortName: "Harris\u2013Benedict original",
    author: "Harris y Benedict",
    year: 1919,
    catalogVersion: 1,
    methodVersion: "1.0.0",
    kind: "predictive_equation",
    resultType: "BMR",
    resultUnit: "kcal/day",
    requiredInputs: [...commonInputs],
    applicability: {
      population: "Poblaci\xF3n estudiada por Harris y Benedict; interpretar seg\xFAn contexto cl\xEDnico.",
      sexScope: "male_and_female"
    },
    variants: [
      {
        code: "male",
        label: "Hombre",
        when: { sex: "male" },
        equation: {
          kind: "linear",
          intercept: 66.473,
          coefficients: { weightKg: 13.7516, heightCm: 5.0033, ageYears: -6.755 },
          sourceUnit: "kcal/day",
          expression: "66.4730 + 13.7516 \xD7 peso_kg + 5.0033 \xD7 talla_cm \u2212 6.7550 \xD7 edad_a\xF1os"
        }
      },
      {
        code: "female",
        label: "Mujer",
        when: { sex: "female" },
        equation: {
          kind: "linear",
          intercept: 655.0955,
          coefficients: { weightKg: 9.5634, heightCm: 1.8496, ageYears: -4.6756 },
          sourceUnit: "kcal/day",
          expression: "655.0955 + 9.5634 \xD7 peso_kg + 1.8496 \xD7 talla_cm \u2212 4.6756 \xD7 edad_a\xF1os"
        }
      }
    ],
    references: [references.harrisOriginal],
    notes: ["Se conserva como m\xE9todo independiente de la revisi\xF3n de 1984."],
    warnings: ["La antig\xFCedad y poblaci\xF3n original deben considerarse al interpretar el resultado."],
    active: true
  },
  {
    code: "HARRIS_BENEDICT_ROZA_SHIZGAL_1984",
    name: "Harris\u2013Benedict revisada por Roza y Shizgal 1984",
    shortName: "Harris\u2013Benedict revisada",
    author: "Roza y Shizgal",
    year: 1984,
    catalogVersion: 1,
    methodVersion: "1.0.0",
    kind: "predictive_equation",
    resultType: "REE",
    resultUnit: "kcal/day",
    requiredInputs: [...commonInputs],
    applicability: {
      population: "Poblaci\xF3n de la reevaluaci\xF3n de Roza y Shizgal.",
      sexScope: "male_and_female"
    },
    variants: [
      {
        code: "male",
        label: "Hombre",
        when: { sex: "male" },
        equation: {
          kind: "linear",
          intercept: 88.362,
          coefficients: { weightKg: 13.397, heightCm: 4.799, ageYears: -5.677 },
          sourceUnit: "kcal/day",
          expression: "88.362 + 13.397 \xD7 peso_kg + 4.799 \xD7 talla_cm \u2212 5.677 \xD7 edad_a\xF1os"
        }
      },
      {
        code: "female",
        label: "Mujer",
        when: { sex: "female" },
        equation: {
          kind: "linear",
          intercept: 447.593,
          coefficients: { weightKg: 9.247, heightCm: 3.098, ageYears: -4.33 },
          sourceUnit: "kcal/day",
          expression: "447.593 + 9.247 \xD7 peso_kg + 3.098 \xD7 talla_cm \u2212 4.330 \xD7 edad_a\xF1os"
        }
      }
    ],
    references: [references.rozaShizgal],
    notes: ["No se presenta como equivalente silencioso de Harris\u2013Benedict 1919."],
    warnings: ["Es una estimaci\xF3n predictiva; revisar aplicabilidad individual."],
    active: true
  },
  {
    code: "VALENCIA_MEXICO",
    name: "Valencia \xB7 poblaci\xF3n mexicana",
    shortName: "Valencia",
    author: "Valencia y colaboradores",
    year: 1994,
    catalogVersion: 1,
    methodVersion: "1.0.0",
    kind: "predictive_equation",
    resultType: "BMR",
    resultUnit: "kcal/day",
    requiredInputs: ["weightKg", "ageYears", "sex"],
    applicability: {
      population: "Poblaci\xF3n mexicana; las variantes configuradas comienzan a los 18 a\xF1os.",
      minAge: 18,
      sexScope: "male_and_female"
    },
    variants: [
      ["MALE_18_30", "Hombre \xB7 18 a <30 a\xF1os", "male", 18, 30, false, 13.37, 747],
      ["MALE_30_60", "Hombre \xB7 30 a 60 a\xF1os", "male", 30, 60, true, 13.08, 693],
      ["MALE_OVER_60", "Hombre \xB7 m\xE1s de 60 a\xF1os", "male", 60, void 0, false, 14.21, 429],
      ["FEMALE_18_30", "Mujer \xB7 18 a <30 a\xF1os", "female", 18, 30, false, 11.02, 679],
      ["FEMALE_30_60", "Mujer \xB7 30 a 60 a\xF1os", "female", 30, 60, true, 10.92, 677],
      ["FEMALE_OVER_60", "Mujer \xB7 m\xE1s de 60 a\xF1os", "female", 60, void 0, false, 10.98, 520]
    ].map(([code, label, sex, minAge, maxAge, maxInclusive, coefficient, intercept]) => ({
      code: String(code),
      label: String(label),
      when: {
        sex,
        minAge: Number(minAge),
        maxAge: maxAge === void 0 ? void 0 : Number(maxAge),
        minAgeInclusive: code === "MALE_OVER_60" || code === "FEMALE_OVER_60" ? false : true,
        maxAgeInclusive: Boolean(maxInclusive)
      },
      equation: {
        kind: "linear",
        intercept: Number(intercept),
        coefficients: { weightKg: Number(coefficient) },
        sourceUnit: "kcal/day",
        expression: `${coefficient} \xD7 peso_kg + ${intercept}`
      }
    })),
    references: [references.valencia],
    notes: ["La variante se selecciona y se conserva de manera expl\xEDcita seg\xFAn edad y sexo."],
    warnings: ["No extrapolar autom\xE1ticamente una variante adulta a menores de 18 a\xF1os."],
    active: true
  },
  {
    code: "SCHOFIELD_WEIGHT_1985",
    name: "Schofield 1985 \xB7 ecuaciones basadas en peso",
    shortName: "Schofield por peso",
    author: "Schofield",
    year: 1985,
    catalogVersion: 1,
    methodVersion: "1.0.0",
    kind: "predictive_equation",
    resultType: "BMR",
    resultUnit: "kcal/day",
    requiredInputs: ["weightKg", "ageYears", "sex"],
    applicability: {
      population: "Grupos por edad y sexo del conjunto revisado por Schofield.",
      minAge: 0,
      sexScope: "male_and_female"
    },
    variants: [
      ["MALE_0_3", "Hombre \xB7 0 a <3 a\xF1os", "male", 0, 3, 0.249, -0.127],
      ["MALE_3_10", "Hombre \xB7 3 a <10 a\xF1os", "male", 3, 10, 0.095, 2.11],
      ["MALE_10_18", "Hombre \xB7 10 a <18 a\xF1os", "male", 10, 18, 0.074, 2.754],
      ["MALE_18_30", "Hombre \xB7 18 a <30 a\xF1os", "male", 18, 30, 0.063, 2.896],
      ["MALE_30_60", "Hombre \xB7 30 a <60 a\xF1os", "male", 30, 60, 0.048, 3.653],
      ["MALE_60_PLUS", "Hombre \xB7 60 a\xF1os o m\xE1s", "male", 60, void 0, 0.049, 2.459],
      ["FEMALE_0_3", "Mujer \xB7 0 a <3 a\xF1os", "female", 0, 3, 0.244, -0.13],
      ["FEMALE_3_10", "Mujer \xB7 3 a <10 a\xF1os", "female", 3, 10, 0.085, 2.033],
      ["FEMALE_10_18", "Mujer \xB7 10 a <18 a\xF1os", "female", 10, 18, 0.056, 2.898],
      ["FEMALE_18_30", "Mujer \xB7 18 a <30 a\xF1os", "female", 18, 30, 0.062, 2.036],
      ["FEMALE_30_60", "Mujer \xB7 30 a <60 a\xF1os", "female", 30, 60, 0.034, 3.538],
      ["FEMALE_60_PLUS", "Mujer \xB7 60 a\xF1os o m\xE1s", "female", 60, void 0, 0.038, 2.755]
    ].map(([code, label, sex, minAge, maxAge, coefficient, intercept]) => ({
      code: String(code),
      label: String(label),
      when: {
        sex,
        minAge: Number(minAge),
        maxAge: maxAge === void 0 ? void 0 : Number(maxAge),
        minAgeInclusive: true,
        maxAgeInclusive: false
      },
      equation: {
        kind: "linear",
        intercept: Number(intercept),
        coefficients: { weightKg: Number(coefficient) },
        sourceUnit: "MJ/day",
        expression: `${coefficient} \xD7 peso_kg ${Number(intercept) < 0 ? "\u2212" : "+"} ${Math.abs(Number(intercept))} MJ/d\xEDa`
      }
    })),
    references: [references.schofield, references.fao],
    relatedMethodCode: "FAO_WHO_UNU_FRAMEWORK",
    notes: [`La salida original en MJ/d\xEDa se convierte al final con 1 MJ = ${MJ_TO_KCAL} kcal.`],
    warnings: ["El error de predicci\xF3n individual puede ser considerable."],
    active: true
  },
  {
    code: "FAO_WHO_UNU_FRAMEWORK",
    name: "Marco FAO/WHO/UNU de requerimientos energ\xE9ticos",
    shortName: "FAO/WHO/UNU",
    author: "FAO, WHO y UNU",
    year: 2004,
    catalogVersion: 1,
    methodVersion: "1.0.0",
    kind: "reference_framework",
    resultType: "framework",
    resultUnit: null,
    requiredInputs: [],
    applicability: { population: "Poblaciones a lo largo del ciclo de vida.", sexScope: "male_and_female" },
    variants: [],
    references: [references.fao],
    relatedMethodCode: "SCHOFIELD_WEIGHT_1985",
    notes: ["Es un marco de referencia que combina BMR y PAL; no duplica Schofield como otra ecuaci\xF3n."],
    warnings: [],
    active: true
  },
  {
    code: "MANUAL_ENERGY_TARGET",
    name: "Objetivo energ\xE9tico manual",
    shortName: "Objetivo manual",
    author: "Nuthrick",
    year: null,
    catalogVersion: 1,
    methodVersion: "1.0.0",
    kind: "manual_target",
    resultType: "energy_target",
    resultUnit: "kcal/day",
    requiredInputs: [],
    applicability: { population: "Definido por el profesional.", sexScope: "male_and_female" },
    variants: [],
    references: [],
    notes: ["Representa una prescripci\xF3n directa; no calcula GER o GEB."],
    warnings: [],
    active: true
  },
  {
    code: "MEASURED_INDIRECT_CALORIMETRY",
    name: "Calorimetr\xEDa indirecta medida",
    shortName: "Calorimetr\xEDa indirecta",
    author: "Nuthrick",
    year: null,
    catalogVersion: 1,
    methodVersion: "1.0.0",
    kind: "measured",
    resultType: "REE",
    resultUnit: "kcal/day",
    requiredInputs: [],
    applicability: { population: "Seg\xFAn protocolo, equipo y condiciones de medici\xF3n.", sexScope: "male_and_female" },
    variants: [],
    references: [],
    notes: ["Preparado para registrar valor, fecha y fuente/equipo en un objetivo posterior."],
    warnings: ["Debe etiquetarse como medido, no como resultado de una ecuaci\xF3n."],
    active: true
  }
];
var predictiveEnergyFormulaCodes = energyMethodCatalog.filter((method) => method.kind === "predictive_equation").map((method) => method.code);
var activityMethodCatalog = [
  {
    code: "CLINICAL_ACTIVITY_FACTOR",
    name: "Factor cl\xEDnico configurable",
    methodVersion: "1.0.0",
    kind: "factor",
    includesEta: false,
    description: "Desglosa la energ\xEDa de actividad como GER \xD7 (factor \u2212 1)."
  },
  {
    code: "PAL_FAO_WHO_UNU",
    name: "PAL FAO/WHO/UNU",
    methodVersion: "1.0.0",
    kind: "pal",
    includesEta: true,
    description: "Calcula GET como GER \xD7 PAL; el PAL representa TEE/BMR e integra ETA.",
    reference: references.fao
  }
];

// frontend/src/features/diet-energy/model.ts
function isPlanEnergyTargetValid(target) {
  return target !== null && Number.isFinite(target) && target > 0 && target <= 1e4;
}

// frontend/src/features/macros/catalog.ts
var macroCatalog = [
  { code: "CARBOHYDRATE", label: "Carbohidratos", shortLabel: "CHO", kcalPerGram: 4, color: "#4d8bdb" },
  { code: "PROTEIN", label: "Prote\xEDna", shortLabel: "PRO", kcalPerGram: 4, color: "#8a62bf" },
  { code: "FAT", label: "Grasas", shortLabel: "GRASA", kcalPerGram: 9, color: "#d99032" }
];
var MACRO_ENERGY_TOLERANCE_KCAL = 1;
var MACRO_PERCENTAGE_TOLERANCE = 1e-6;
function getMacroCatalogEntry(code) {
  const macro = macroCatalog.find((item) => item.code === code);
  if (!macro) throw new Error(`Macronutriente no registrado: ${code}`);
  return macro;
}

// frontend/src/features/macros/model.ts
var macroCodes = macroCatalog.map((item) => item.code);
var finiteNonNegative = (value) => value !== null && value !== void 0 && Number.isFinite(value) && value >= 0;
function blankMacro(code) {
  return {
    code,
    input_mode: "percentage",
    input_value: null,
    percentage: null,
    kcal: null,
    grams: null,
    grams_per_kg: null
  };
}
function calculatedMacro(macro, targetEnergyKcal, referenceWeightKg) {
  const factor = getMacroCatalogEntry(macro.code).kcalPerGram;
  const input = finiteNonNegative(macro.input_value) ? macro.input_value : null;
  if (input === null || targetEnergyKcal <= 0) return { ...macro, input_value: input, percentage: null, kcal: null, grams: null, grams_per_kg: null };
  let kcal = null;
  if (macro.input_mode === "percentage") kcal = targetEnergyKcal * input / 100;
  if (macro.input_mode === "grams") kcal = input * factor;
  if (macro.input_mode === "grams_per_kg" && referenceWeightKg !== null) kcal = input * referenceWeightKg * factor;
  if (kcal === null) return { ...macro, input_value: input, percentage: null, kcal: null, grams: null, grams_per_kg: null };
  const grams = kcal / factor;
  return {
    ...macro,
    input_value: input,
    kcal,
    grams,
    percentage: kcal / targetEnergyKcal * 100,
    grams_per_kg: referenceWeightKg && referenceWeightKg > 0 ? grams / referenceWeightKg : null
  };
}
function calculateMacroDistribution(draft) {
  const target = finiteNonNegative(draft.target_energy_kcal) ? draft.target_energy_kcal : 0;
  const referenceWeight = finiteNonNegative(draft.reference_weight_kg) && draft.reference_weight_kg > 0 ? draft.reference_weight_kg : null;
  const macros = Object.fromEntries(
    macroCodes.map((code) => [code, calculatedMacro(draft.macros[code] ?? blankMacro(code), target, referenceWeight)])
  );
  const entries = macroCodes.map((code) => macros[code]);
  const allCalculated = entries.every((macro) => macro.kcal !== null && macro.percentage !== null);
  const totalKcal = entries.reduce((sum, macro) => sum + (macro.kcal ?? 0), 0);
  const totalPercentage = entries.reduce((sum, macro) => sum + (macro.percentage ?? 0), 0);
  const allPercentage = entries.every((macro) => macro.input_mode === "percentage");
  const complete = allCalculated && Math.abs(target - totalKcal) <= MACRO_ENERGY_TOLERANCE_KCAL && (!allPercentage || Math.abs(totalPercentage - 100) <= MACRO_PERCENTAGE_TOLERANCE);
  return {
    ...draft,
    target_energy_kcal: target,
    reference_weight_kg: referenceWeight,
    reference_weight_source: referenceWeight === null ? "unavailable" : draft.reference_weight_source,
    macros,
    totals: { percentage: totalPercentage, kcal: totalKcal, difference_kcal: target - totalKcal },
    complete,
    updated_at: (/* @__PURE__ */ new Date()).toISOString()
  };
}

// frontend/src/features/exchanges/catalog.ts
var EXCHANGE_SYSTEM_CODE = "SMAE_NOM037_2012";
var EXCHANGE_CATALOG_VERSION = "1.0.0";
var group = (groupCode, groupName, shortName, category, categoryName, displayOrder, energyKcal, carbohydrateG, proteinG, fatG) => ({
  systemCode: EXCHANGE_SYSTEM_CODE,
  catalogVersion: EXCHANGE_CATALOG_VERSION,
  groupCode,
  groupName,
  shortName,
  category,
  categoryName,
  displayOrder,
  energyKcal,
  carbohydrateG,
  proteinG,
  fatG,
  active: true,
  source: "SMAE / NOM-037-SSA2-2012"
});
var exchangeCatalog = [
  group("VEGETABLES", "Verduras", "Verduras", "produce", "Vegetales y frutas", 10, 25, 4, 2, 0),
  group("FRUITS", "Frutas", "Frutas", "produce", "Vegetales y frutas", 20, 60, 15, 0, 0),
  group("CEREALS_NO_FAT", "Cereales y tub\xE9rculos sin grasa", "Cereales sin grasa", "grains", "Cereales y leguminosas", 30, 70, 15, 2, 0),
  group("CEREALS_WITH_FAT", "Cereales y tub\xE9rculos con grasa", "Cereales con grasa", "grains", "Cereales y leguminosas", 40, 115, 15, 2, 5),
  group("LEGUMES", "Leguminosas", "Leguminosas", "grains", "Cereales y leguminosas", 50, 120, 20, 8, 1),
  group("AOA_VERY_LOW_FAT", "AOA \xB7 muy bajos en grasa", "AOA muy bajos", "animal", "Alimentos de origen animal", 60, 40, 0, 7, 1),
  group("AOA_LOW_FAT", "AOA \xB7 bajos en grasa", "AOA bajos", "animal", "Alimentos de origen animal", 70, 55, 0, 7, 3),
  group("AOA_MODERATE_FAT", "AOA \xB7 moderados en grasa", "AOA moderados", "animal", "Alimentos de origen animal", 80, 75, 0, 7, 5),
  group("AOA_HIGH_FAT", "AOA \xB7 altos en grasa", "AOA altos", "animal", "Alimentos de origen animal", 90, 100, 0, 7, 8),
  group("MILK_SKIM", "Leche descremada", "Descremada", "milk", "Leches", 100, 95, 12, 9, 2),
  group("MILK_SEMI_SKIM", "Leche semidescremada", "Semidescremada", "milk", "Leches", 110, 110, 12, 9, 4),
  group("MILK_WHOLE", "Leche entera", "Entera", "milk", "Leches", 120, 150, 12, 9, 8),
  group("MILK_WITH_SUGAR", "Leche con az\xFAcar", "Con az\xFAcar", "milk", "Leches", 130, 200, 30, 8, 8),
  group("FATS_NO_PROTEIN", "Aceites y grasas sin prote\xEDna", "Sin prote\xEDna", "fats", "Grasas", 140, 45, 0, 0, 5),
  group("FATS_WITH_PROTEIN", "Aceites y grasas con prote\xEDna", "Con prote\xEDna", "fats", "Grasas", 150, 70, 3, 3, 5),
  group("SUGARS_NO_FAT", "Az\xFAcares sin grasa", "Sin grasa", "sugars", "Az\xFAcares", 160, 40, 10, 0, 0),
  group("SUGARS_WITH_FAT", "Az\xFAcares con grasa", "Con grasa", "sugars", "Az\xFAcares", 170, 85, 10, 0, 5)
];
var exchangeCatalogCategories = [...new Map(exchangeCatalog.map((item) => [item.category, item.categoryName])).entries()].map(([category, label]) => ({ category, label }));
function getExchangeGroup(code) {
  const result = exchangeCatalog.find((group2) => group2.groupCode === code);
  if (!result) throw new Error(`Grupo de equivalentes no encontrado: ${code}`);
  return result;
}

// frontend/src/features/meal-distribution/model.ts
var EPSILON = 1e-7;
var round = (value) => Math.round((value + Number.EPSILON) * 1e6) / 1e6;
var zeroTotals = () => ({ energy_kcal: 0, carbohydrate_g: 0, protein_g: 0, fat_g: 0 });
function sameExchangeInventory(snapshot, prescription) {
  if (!snapshot || snapshot.exchange_system_code !== prescription.exchange_system_code || snapshot.catalog_version !== prescription.catalog_version) return false;
  const current = new Map(prescription.groups.map((group2) => [group2.group_code, group2.portions]));
  return snapshot.groups.every((group2) => Math.abs((current.get(group2.group_code) ?? 0) - group2.portions) <= EPSILON) && prescription.groups.every((group2) => snapshot.groups.some((item) => item.group_code === group2.group_code));
}
function calculateGroupDistribution(distribution, groupCode) {
  return round(distribution.filter((entry) => entry.group_code === groupCode).reduce((sum, entry) => sum + entry.portions, 0));
}
function calculateRemainingExchanges(distribution, prescription, groupCode) {
  const available = prescription.groups.find((group2) => group2.group_code === groupCode)?.portions ?? 0;
  const assigned = calculateGroupDistribution(distribution, groupCode);
  return { available, assigned, remaining: round(available - assigned) };
}
function calculateMealNutrition(distribution, mealTimeId) {
  const total = distribution.filter((entry) => entry.meal_time_id === mealTimeId).reduce((sum, entry) => {
    const group2 = getExchangeGroup(entry.group_code);
    return {
      energy_kcal: sum.energy_kcal + entry.portions * group2.energyKcal,
      carbohydrate_g: sum.carbohydrate_g + entry.portions * group2.carbohydrateG,
      protein_g: sum.protein_g + entry.portions * group2.proteinG,
      fat_g: sum.fat_g + entry.portions * group2.fatG
    };
  }, zeroTotals());
  return { meal_time_id: mealTimeId, ...Object.fromEntries(Object.entries(total).map(([key, value]) => [key, round(value)])) };
}
function calculateDerivedMealTotals(distribution, mealTimes) {
  return [...mealTimes].sort((a, b) => a.display_order - b.display_order).map((meal) => calculateMealNutrition(distribution, meal.id));
}
function calculateDistributionStatus(distribution, prescription) {
  const groups3 = exchangeCatalog.map((catalog) => {
    const values = calculateRemainingExchanges(distribution, prescription, catalog.groupCode);
    const state = values.remaining < -EPSILON ? "excess" : values.remaining > EPSILON ? "pending" : "complete";
    return { group_code: catalog.groupCode, ...values, state };
  }).filter((group2) => group2.available > EPSILON || group2.assigned > EPSILON);
  const complete = groups3.filter((group2) => group2.state === "complete").length;
  const pending = groups3.filter((group2) => group2.state === "pending").length;
  const excess = groups3.filter((group2) => group2.state === "excess").length;
  return {
    groups: groups3,
    complete,
    pending,
    excess,
    canConfirm: groups3.length > 0 && pending === 0 && excess === 0,
    availablePortions: round(groups3.reduce((sum, group2) => sum + group2.available, 0)),
    assignedPortions: round(groups3.reduce((sum, group2) => sum + group2.assigned, 0)),
    remainingPortions: round(groups3.reduce((sum, group2) => sum + group2.remaining, 0))
  };
}

// frontend/src/features/menu/units.ts
var foodUnitLabels = {
  g: "g",
  ml: "ml",
  piece: "pieza",
  half: "mitades",
  cup: "taza",
  tablespoon: "cucharada",
  teaspoon: "cucharadita",
  slice: "rebanada",
  tortilla: "tortilla",
  glass: "vaso",
  serving: "porci\xF3n",
  unit: "unidad",
  recipe_serving: "porci\xF3n"
};
function normalizeCatalogPortion(food) {
  const fraction = food.portion_fraction;
  return fraction && fraction.numerator > 0 && fraction.denominator > 0 ? { ...food, portion_amount: fraction.numerator / fraction.denominator } : food;
}

// frontend/src/features/exchanges/model.ts
var zeroTotals2 = () => ({ energy_kcal: 0, carbohydrate_g: 0, protein_g: 0, fat_g: 0 });
function sameExchangeTargets(a, b) {
  return a.energy_kcal === b.energy_kcal && a.carbohydrate_g === b.carbohydrate_g && a.protein_g === b.protein_g && a.fat_g === b.fat_g;
}
function calculateExchangeTotals(groups3) {
  return groups3.reduce((totals, item) => {
    const catalog = getExchangeGroup(item.group_code);
    return {
      energy_kcal: totals.energy_kcal + item.portions * catalog.energyKcal,
      carbohydrate_g: totals.carbohydrate_g + item.portions * catalog.carbohydrateG,
      protein_g: totals.protein_g + item.portions * catalog.proteinG,
      fat_g: totals.fat_g + item.portions * catalog.fatG
    };
  }, zeroTotals2());
}
function calculateExchangeDifferences(actual, target) {
  return {
    energy_kcal: actual.energy_kcal - target.energy_kcal,
    carbohydrate_g: actual.carbohydrate_g - target.carbohydrate_g,
    protein_g: actual.protein_g - target.protein_g,
    fat_g: actual.fat_g - target.fat_g
  };
}

// frontend/src/features/menu/model.ts
var DIET_MENU_SCHEMA_VERSION = 1;
var MENU_COMPARISON_TOLERANCE = 0.1;
var MENU_NUMERIC_EPSILON = 1e-6;
var now = () => (/* @__PURE__ */ new Date()).toISOString();
var roundMenuNumber = (value) => Math.round((value + Number.EPSILON) * 1e6) / 1e6;
var round2 = roundMenuNumber;
var makeId = (prefix) => globalThis.crypto?.randomUUID?.() ?? `${prefix}-${Date.now()}-${Math.random().toString(36).slice(2)}`;
function createFoodSnapshot(food) {
  return {
    id: food.id,
    name: food.name,
    group_code: food.group_code,
    portion_amount: Number(food.portion_amount),
    portion_unit: food.portion_unit,
    portion_description: food.portion_description,
    exchange_system_code: food.exchange_system_code,
    exchange_catalog_version: food.exchange_catalog_version,
    source: food.source,
    source_version: food.source_version,
    is_custom: food.is_custom,
    attributes: { ...food.attributes }
  };
}
function exchangeContributionForFood(food, amount) {
  const portions = Number(food.portion_amount) > 0 ? round2(amount / Number(food.portion_amount)) : 0;
  return portions > MENU_NUMERIC_EPSILON ? [{ group_code: food.group_code, portions }] : [];
}
function aggregateContributions(contributions) {
  const totals = /* @__PURE__ */ new Map();
  for (const item of contributions) totals.set(item.group_code, round2((totals.get(item.group_code) ?? 0) + item.portions));
  return [...totals.entries()].map(([group_code, portions]) => ({ group_code, portions })).filter((item) => item.portions > MENU_NUMERIC_EPSILON);
}
function recipeExchangeContributions(recipe, servings = 1) {
  const factor = servings / Number(recipe.servings || 1);
  return aggregateContributions(recipe.items.flatMap((item) => item.exchange_contribution.map((value) => ({
    group_code: value.group_code,
    portions: round2(value.portions * factor)
  }))));
}
function createDietMenu(mealDistribution, idFactory = makeId) {
  const activeMenuId = idFactory("menu");
  return {
    schema_version: DIET_MENU_SCHEMA_VERSION,
    source_meal_distribution_snapshot: null,
    menus: [{
      id: activeMenuId,
      name: "Men\xFA principal",
      display_order: 0,
      meal_menus: mealDistribution.meal_times.map((meal) => ({ meal_time_id: meal.id, entries: [] }))
    }],
    active_menu_id: activeMenuId,
    derived_exchange_usage: [],
    status: "not_started",
    confirmed_at: null,
    updated_at: now()
  };
}
function activeMenu(menu) {
  return menu.menus.find((variant) => variant.id === menu.active_menu_id) ?? menu.menus[0];
}
function withVariant(menu, variant) {
  const menus = menu.menus.map((item) => item.id === variant.id ? variant : item);
  const next = { ...menu, menus, confirmed_at: null, updated_at: now() };
  const usage = calculateMenuUsage(next);
  return {
    ...next,
    derived_exchange_usage: usage,
    status: usage.length ? "editing" : "not_started"
  };
}
function ensureMealMenus(variant, mealDistribution) {
  const validIds = new Set(mealDistribution.meal_times.map((meal) => meal.id));
  const existing = variant.meal_menus.filter((meal) => validIds.has(meal.meal_time_id));
  for (const meal of mealDistribution.meal_times) {
    if (!existing.some((item) => item.meal_time_id === meal.id)) existing.push({ meal_time_id: meal.id, entries: [] });
  }
  return { ...variant, meal_menus: existing };
}
function addFoodToMenu(menu, mealDistribution, mealTimeId, food, quantity, id = makeId("entry")) {
  if (!Number.isFinite(quantity) || quantity <= 0) return menu;
  const entry = {
    id,
    type: "food",
    source_id: food.id,
    name_snapshot: food.name,
    quantity: round2(quantity),
    unit: food.portion_unit,
    food_snapshot: createFoodSnapshot(food),
    exchange_contributions: exchangeContributionForFood(food, quantity)
  };
  const variant = ensureMealMenus(activeMenu(menu), mealDistribution);
  return withVariant(menu, { ...variant, meal_menus: variant.meal_menus.map((meal) => meal.meal_time_id === mealTimeId ? { ...meal, entries: [...meal.entries, entry] } : meal) });
}
function addRecipeToMenu(menu, mealDistribution, mealTimeId, recipe, servings = 1, id = makeId("entry")) {
  if (!Number.isFinite(servings) || servings <= 0) return menu;
  const verifiedWater = recipe.id === "nuthrick-water-v1" && recipe.source === "CDC_PLAIN_WATER" && recipe.tags?.includes("nuthrick:drink");
  if (!Number.isFinite(Number(recipe.servings)) || Number(recipe.servings) <= 0 || !recipe.items.length && !verifiedWater || recipe.items.some((item) => !Number.isFinite(Number(item.amount)) || Number(item.amount) <= 0 || !item.exchange_contribution.length)) return menu;
  const entry = {
    id,
    type: "recipe",
    source_id: recipe.id,
    name_snapshot: recipe.name,
    quantity: round2(servings),
    unit: "recipe_serving",
    culinary_role: recipe.tags?.includes("nuthrick:drink") ? "drink" : void 0,
    recipe_snapshot: {
      recipe_id: recipe.id,
      name: recipe.name,
      servings: recipe.servings,
      instructions: recipe.instructions,
      tags: [...recipe.tags ?? []],
      substitution_notes: recipe.substitution_notes,
      source_reference: recipe.source_reference,
      source: recipe.source,
      items: recipe.items.map((item) => ({
        amount: Number(item.amount),
        unit: item.unit,
        food_snapshot: structuredClone(item.food_snapshot),
        exchange_contribution: item.exchange_contribution.map((value) => ({ ...value }))
      }))
    },
    exchange_contributions: recipeExchangeContributions(recipe, servings)
  };
  const variant = ensureMealMenus(activeMenu(menu), mealDistribution);
  return withVariant(menu, { ...variant, meal_menus: variant.meal_menus.map((meal) => meal.meal_time_id === mealTimeId ? { ...meal, entries: [...meal.entries, entry] } : meal) });
}
function calculateMenuUsage(menu) {
  const variant = activeMenu(menu);
  if (!variant) return [];
  const totals = /* @__PURE__ */ new Map();
  for (const meal of variant.meal_menus) for (const entry of meal.entries) for (const contribution of entry.exchange_contributions) {
    const key = `${meal.meal_time_id}:${contribution.group_code}`;
    const previous = totals.get(key);
    totals.set(key, { meal_time_id: meal.meal_time_id, group_code: contribution.group_code, portions: round2((previous?.portions ?? 0) + contribution.portions) });
  }
  return [...totals.values()];
}
function calculateMenuStatus(menu, mealDistribution) {
  const usage = calculateMenuUsage(menu);
  const rows = mealDistribution.distribution.filter((item) => item.portions > 0).map((required) => {
    const used = usage.find((item) => item.meal_time_id === required.meal_time_id && item.group_code === required.group_code)?.portions ?? 0;
    const remaining = round2(required.portions - used);
    return {
      ...required,
      used,
      remaining,
      state: remaining > MENU_COMPARISON_TOLERANCE ? "pending" : remaining < -MENU_COMPARISON_TOLERANCE ? "excess" : "complete"
    };
  });
  for (const used of usage) {
    if (!rows.some((row) => row.meal_time_id === used.meal_time_id && row.group_code === used.group_code) && used.portions > MENU_COMPARISON_TOLERANCE) {
      rows.push({ ...used, portions: 0, used: used.portions, remaining: -used.portions, state: "excess" });
    }
  }
  return {
    rows,
    complete: rows.filter((row) => row.state === "complete").length,
    pending: rows.filter((row) => row.state === "pending").length,
    excess: rows.filter((row) => row.state === "excess").length,
    canConfirm: rows.length > 0 && rows.every((row) => row.state === "complete")
  };
}

// frontend/src/features/diet-workshop/clinicalSummary.ts
var object = (v) => !!v && typeof v === "object" && !Array.isArray(v);
var positive = (v) => typeof v === "number" && Number.isFinite(v) && v > 0;
var groups = new Set(exchangeCatalog.map((g) => g.groupCode));
var CLINICAL_SUMMARY_LIMITS = { meals: 8, foodsPerMeal: 8, foodCharacters: 64, recordedItems: 150 };
function summarizeConfirmedRecall(raw, sanitize) {
  if (!object(raw) || typeof raw.approved_at !== "string" || !Number.isFinite(Date.parse(raw.approved_at)) || !Array.isArray(raw.items) || !raw.items.length || raw.items.length > CLINICAL_SUMMARY_LIMITS.recordedItems) return void 0;
  const clean = (s) => sanitize(s).trim().slice(0, CLINICAL_SUMMARY_LIMITS.foodCharacters);
  const contributions = [];
  const meals = /* @__PURE__ */ new Map();
  for (const item of raw.items) {
    if (!object(item) || !object(item.food) || typeof item.mealLabel !== "string" || !item.mealLabel.trim() || typeof item.food.name !== "string" || !item.food.name.trim() || !positive(item.quantity) || item.quantity > 1e4 || !positive(item.food.portion_amount) || item.unit !== item.food.portion_unit || typeof item.food.group_code !== "string" || !groups.has(item.food.group_code)) return void 0;
    contributions.push(...exchangeContributionForFood(item.food, item.quantity));
    const meal = clean(item.mealLabel), food = clean(item.food.name);
    if (!meals.has(meal)) meals.set(meal, /* @__PURE__ */ new Set());
    meals.get(meal).add(food);
  }
  const totals = calculateExchangeTotals(contributions);
  if (!Object.values(totals).every((v) => typeof v === "number" && Number.isFinite(v))) return void 0;
  const truncated = meals.size > CLINICAL_SUMMARY_LIMITS.meals || [...meals.values()].some((f) => f.size > CLINICAL_SUMMARY_LIMITS.foodsPerMeal);
  return {
    scope: "one_recorded_day_not_prescription",
    totalsScope: "confirmed_items_only",
    nutrition: totals,
    meals: [...meals].slice(0, CLINICAL_SUMMARY_LIMITS.meals).map(([name, foods]) => ({ name, foods: [...foods].slice(0, CLINICAL_SUMMARY_LIMITS.foodsPerMeal) })),
    ...truncated ? { foodListTruncated: true } : {}
  };
}
function summarizeAnthropometry(raw) {
  if (!object(raw)) return void 0;
  const keys = ["weightKg", "heightCm", "bmi", "waistCm", "bodyFatPct", "leanMassKg"];
  const result = Object.fromEntries(keys.flatMap((k) => positive(raw[k]) && (k !== "bodyFatPct" || raw[k] <= 100) ? [[k, raw[k]]] : []));
  return Object.keys(result).length ? result : void 0;
}

// frontend/src/features/diet-workshop/generationContext.ts
function contextFact(raw, valid) {
  if (raw === void 0) return { state: "unavailable", reason: "missing" };
  if (raw === null) return { state: "unavailable", reason: "null" };
  if (typeof raw === "string" && !raw.trim()) return { state: "unavailable", reason: "blank" };
  if (raw === "No sabe / no recuerda") return { state: "unknown", reason: "not_recalled" };
  if (raw === "Prefiere no responder") return { state: "unknown", reason: "declined" };
  if (raw === "No aplica") return { state: "not_applicable", reason: "explicit" };
  if (Array.isArray(raw) && raw.length === 0) return { state: "unavailable", reason: "empty_array" };
  if (Array.isArray(raw) && raw.length === 1 && ["No sabe / no recuerda", "Prefiere no responder", "No aplica"].includes(raw[0]))
    return contextFact(raw[0], valid);
  return valid(raw) ? { state: "known", value: raw } : { state: "unavailable", reason: "invalid" };
}
var stringValue = (v) => typeof v === "string" && Boolean(v.trim());
var stringList = (v) => Array.isArray(v) && v.length > 0 && v.every(stringValue);
var record = (v) => Boolean(v) && typeof v === "object" && !Array.isArray(v);
var positive2 = (v) => typeof v === "number" && Number.isFinite(v) && v > 0;
var list = (guard) => (v) => Array.isArray(v) && v.length > 0 && v.every(guard);
var reaction = (v) => record(v) && stringValue(v.food) && stringValue(v.classification);
var preference = (v) => record(v) && stringValue(v.food) && stringValue(v.category);
var scheduleKeys = ["day", "start", "end", "meal_window", "minutes"];
var schedule = (v) => record(v) && scheduleKeys.some((k) => stringValue(v[k])) && scheduleKeys.every((k) => v[k] === void 0 || v[k] === null || typeof v[k] === "string");
var time = (v) => typeof v === "string" && /^([01]\d|2[0-3]):[0-5]\d$/.test(v);
var date = (v) => typeof v === "string" && Number.isFinite(Date.parse(v));
var unavailable = () => ({ state: "unavailable", reason: "missing" });
function mapFact(fact, fn) {
  return fact.state === "known" ? { state: "known", value: fn(fact.value) } : fact;
}
function buildDietGenerationContext(source, sanitizeText) {
  const { plan, consultation: c } = source;
  const blockers = [];
  const clean = (s) => sanitizeText(s.trim());
  const fromPlan = (path, fact, calculated = false) => ({
    fact,
    origin: { source: "nutrition_plans", path, kind: calculated ? "system_calculated" : "professional_captured" }
  });
  const answer = (key, guard, project) => ({
    fact: mapFact(contextFact(source.answers[key]?.value, guard), project),
    origin: {
      source: "consultation_answers",
      path: key,
      kind: source.answers[key]?.response_area === "professional_assessment" ? "professional_captured" : "patient_declared"
    }
  });
  const multiple = (key) => answer(key, stringList, (v) => v.map(clean));
  if (plan.status !== "draft") blockers.push("draft_required");
  if (!plan.patient_id || !plan.consultation_id || !c) blockers.push("consultation_required");
  const sameContext = Boolean(c && c.id === plan.consultation_id && c.patient_id === plan.patient_id && c.professional_id === plan.professional_id && Number.isInteger(c.revision) && c.revision > 0);
  if (c && !sameContext) blockers.push("context_mismatch");
  const energy = contextFact(plan.target_calories, positive2);
  if (!isPlanEnergyTargetValid(plan.target_calories)) blockers.push("energy_required");
  let macros = unavailable();
  const m = plan.macro_distribution;
  if (!m) blockers.push("macros_required");
  else {
    const validInputs = macroCatalog.every(({ code }) => {
      const item = m.macros?.[code];
      return item && item.code === code && ["grams", "percentage", "grams_per_kg"].includes(item.input_mode) && typeof item.input_value === "number" && Number.isFinite(item.input_value) && item.input_value >= 0;
    });
    const calculated = validInputs ? calculateMacroDistribution(m) : null;
    if (!calculated?.complete || m.target_energy_kcal !== plan.target_calories) blockers.push("prescription_inconsistent");
    else macros = { state: "known", value: Object.fromEntries(macroCatalog.map(({ code }) => {
      const item = calculated.macros[code];
      return [code, { grams: item.grams, percentage: item.percentage, kcal: item.kcal }];
    })) };
  }
  if (plan.energy_calculation?.prescribed_target_kcal != null && plan.energy_calculation.prescribed_target_kcal !== plan.target_calories)
    blockers.push("prescription_inconsistent");
  const pesApproved = sameContext && c?.pes && date(c.pes.approved_at) && stringValue(c.pes.statement);
  const goalApproved = pesApproved && c?.objective && date(c.objective.approved_at) && stringValue(c.objective.content) && c.objective.revision === c.revision && c.objective.pes_approved_at === c.pes?.approved_at && c.objective.pes_statement === c.pes?.statement;
  if (!pesApproved) blockers.push("pes_approval_required");
  if (!goalApproved) blockers.push("objective_approval_required");
  const distribution = plan.meal_distribution;
  const meals = distribution?.meal_times;
  if (!meals?.length) blockers.push("meal_structure_required");
  const validMeals = Boolean(meals?.length && new Set(meals.map((v) => v.id)).size === meals.length && meals.every((v) => stringValue(v.id) && stringValue(v.display_name) && ["BREAKFAST", "SNACK", "MAIN_MEAL", "DINNER", "CUSTOM"].includes(v.meal_type) && Number.isInteger(v.display_order) && v.display_order >= 0 && (v.time === null || time(v.time))));
  if (meals?.length && !validMeals) blockers.push("meal_structure_invalid");
  const groups3 = new Set(exchangeCatalog.map((g) => g.groupCode));
  const cells = distribution?.distribution ?? [];
  const validCells = validMeals && cells.every((v) => meals.some((t) => t.id === v.meal_time_id) && groups3.has(v.group_code) && Number.isFinite(v.portions) && v.portions >= 0) && new Set(cells.map((v) => `${v.meal_time_id}:${v.group_code}`)).size === cells.length;
  if (cells.length && !validCells) blockers.push("distribution_invalid");
  const reactionStatus = answer("food_reactions_status", (v) => v === "S\xED" || v === "No", (v) => v);
  const reactions = answer("food_reactions_v2", list(reaction), (rows) => rows.map((r) => ({ food: clean(r.food), classification: clean(r.classification), ...stringValue(r.management) ? { management: clean(r.management) } : {} })));
  const preferences = answer("food_preferences", list(preference), (rows) => rows.map((r) => ({ category: clean(r.category), food: clean(r.food) })));
  const patterns = multiple("eating_preferences");
  const unresolvedReactions = reactions.fact.state === "known" || reactions.fact.state === "unavailable" && reactions.fact.reason === "invalid";
  const unresolvedPreferences = preferences.fact.state === "known" && preferences.fact.value.some((v) => ["No consume", "Preferencia cultural / religiosa"].includes(v.category)) || preferences.fact.state === "unavailable" && preferences.fact.reason === "invalid";
  const unresolvedPattern = patterns.fact.state === "known" && patterns.fact.value.some((v) => ["Vegetariano", "Vegano", "Pescetariano", "Restricci\xF3n religiosa"].includes(v));
  if (reactionStatus.fact.state !== "known" || reactionStatus.fact.value !== "No" || unresolvedReactions || unresolvedPreferences || unresolvedPattern)
    blockers.push("restrictions_need_review");
  if ((source.additionalInstructions?.length ?? 0) > 1200) blockers.push("instructions_too_long");
  const catalogPreferences = Object.entries(plan.diet_menu?.food_preferences ?? {});
  const excludedIds = new Set(catalogPreferences.filter(([, v]) => v === "exclude").map(([id]) => id));
  let catalog = unavailable();
  if (source.catalog && source.catalog.foods.length <= 200 && source.catalog.recipes.length <= 24) {
    const foods = source.catalog.foods.filter((f) => f.active && (f.owner_id === null || f.owner_id === plan.professional_id) && !excludedIds.has(f.id) && positive2(f.portion_amount) && Object.hasOwn(foodUnitLabels, f.portion_unit) && groups3.has(f.group_code));
    const recipes = source.catalog.recipes.filter((r) => r.active && (r.owner_id === null || r.owner_id === plan.professional_id) && positive2(r.servings) && r.items.length > 0 && r.items.every((i) => positive2(i.amount) && foods.some((f) => f.id === i.food_item_id && f.portion_unit === i.unit)));
    const attributeKeys = ["gluten", "lactose", "milk", "egg", "peanut", "tree_nuts", "soy", "fish", "crustaceans", "other"];
    if (foods.length) catalog = { state: "known", value: {
      foods: foods.map((f) => ({
        id: f.id,
        name: clean(f.name),
        group_code: f.group_code,
        portion_amount: f.portion_amount,
        portion_unit: f.portion_unit,
        exchange_system_code: f.exchange_system_code,
        exchange_catalog_version: f.exchange_catalog_version,
        attributes: Object.fromEntries(Object.entries(f.attributes ?? {}).filter(([k, v]) => attributeKeys.includes(k) && ["contains", "free", "unknown"].includes(v)))
      })),
      recipes: recipes.map((r) => ({
        id: r.id,
        name: clean(r.name),
        servings: r.servings,
        instructions: r.instructions === null ? null : clean(r.instructions),
        items: r.items.map((i) => ({ food_item_id: i.food_item_id, amount: i.amount, unit: i.unit }))
      }))
    } };
  }
  if (catalog.state !== "known") blockers.push("catalog_required");
  const recall24h = summarizeConfirmedRecall(source.confirmedRecall, clean);
  const anthropometry = summarizeAnthropometry(source.anthropometry);
  const context = {
    schema_version: 1,
    clinical: {
      ...recall24h ? { recall24h } : {},
      ...anthropometry ? { anthropometry } : {},
      pes: { fact: pesApproved ? { state: "known", value: clean(c.pes.statement) } : unavailable(), origin: { source: "consultation_snapshots", path: "clinical_records.pes + pes_statement", kind: "approved_pes" } },
      objective: { fact: goalApproved ? { state: "known", value: clean(c.objective.content) } : unavailable(), origin: { source: "consultation_snapshots", path: "clinical_records.objective", kind: "approved_objective" } }
    },
    prescription: { energy_kcal: fromPlan("target_calories", energy), macros: fromPlan("macro_distribution.macros.input_value + input_mode", macros, true) },
    meals: fromPlan("meal_distribution.meal_times", validMeals ? { state: "known", value: [...meals].sort((a, b) => a.display_order - b.display_order).map((v) => ({ id: v.id, meal_type: v.meal_type, display_name: clean(v.display_name), time: contextFact(v.time, time), display_order: v.display_order })) } : unavailable()),
    meal_distribution: fromPlan("meal_distribution.distribution", validCells && cells.length ? { state: "known", value: {
      exchanges: cells.map((v) => ({ meal_time_id: v.meal_time_id, group_code: v.group_code, portions: v.portions })),
      totals: calculateDerivedMealTotals(cells, meals)
    } } : unavailable(), true),
    restrictions: { reaction_status: reactionStatus, reactions, excluded_food_ids: catalogPreferences.filter(([, v]) => v === "exclude").map(([id]) => id) },
    preferences: { eating_pattern: patterns, foods: preferences, catalog: catalogPreferences.flatMap(([food_id, v]) => v === "like" || v === "avoid" ? [{ food_id, preference: v }] : []) },
    routine: {
      usual_pattern: multiple("usual_pattern"),
      daily_schedule: answer("daily_schedule", list(schedule), (rows) => rows.map((v) => Object.fromEntries(scheduleKeys.flatMap((k) => stringValue(v[k]) ? [[k, clean(v[k])]] : [])))),
      cooking_time: answer("cooking_time", stringValue, clean),
      food_equipment: multiple("food_equipment")
    },
    professional_instructions: { fact: mapFact(contextFact(source.additionalInstructions, stringValue), clean), origin: { source: "request", path: "additionalInstructions", kind: "professional_captured" } },
    catalog: { fact: catalog, origin: { source: "catalog", path: "food_items + recipes + recipe_items", kind: "catalog_record" } }
  };
  return { context, ready: blockers.length === 0, blockers: [...new Set(blockers)], audit: { plan_id: plan.id, draft_revision: plan.draft_revision, consultation_id: plan.consultation_id, stamp: source.stamp } };
}

// frontend/src/features/exchanges/suggestion.ts
var EXCHANGE_SUGGESTION_INCREMENT = 0.5;

// frontend/src/features/menu/composition.ts
function ingredientsKey(items, factor, ratios = false) {
  const amounts = /* @__PURE__ */ new Map();
  for (const item of items) {
    const key = `${item.food_snapshot.id}:${item.unit}`;
    amounts.set(key, (amounts.get(key) ?? 0) + Number(item.amount) * factor / Number(item.food_snapshot.portion_amount));
  }
  const total = ratios ? [...amounts.values()].reduce((a, b) => a + b, 0) || 1 : 1;
  return [...amounts].sort(([a], [b]) => a.localeCompare(b)).map(([key, value]) => [key, Math.round(value / total * (ratios ? 1e3 : 20))]);
}
function preparationKey(recipe) {
  return JSON.stringify(ingredientsKey(recipe.items, 1, true));
}

// frontend/src/features/diet-workshop/generationCandidates.ts
var DIET_GENERATION_LIMITS = Object.freeze({
  foodsPerMeal: 12,
  recipesPerMeal: 6,
  maxMeals: 12,
  maxEntriesPerMeal: 18,
  maxMultiplier: 8,
  multiplierIncrement: EXCHANGE_SUGGESTION_INCREMENT,
  maxResponseCharacters: 24e3,
  maxPayloadBytes: 2e4,
  textLimit: 1200
});
var dietServingMultipliers = () => Array.from({ length: DIET_GENERATION_LIMITS.maxMultiplier / DIET_GENERATION_LIMITS.multiplierIncrement }, (_, i) => (i + 1) * DIET_GENERATION_LIMITS.multiplierIncrement);
var groups2 = new Set(exchangeCatalog.map((g) => g.groupCode));
var nutrients = ["energy_kcal", "carbohydrate_g", "protein_g", "fat_g"];
var positive3 = (n) => typeof n === "number" && Number.isFinite(n) && n > 0;
var canonicalName = (s) => s.normalize("NFD").replace(/[\u0300-\u036f]/g, "").toLowerCase().trim();
function hardFoodAllowed(food, owner, rules) {
  return food.active === true && (food.owner_id === null || food.owner_id === owner) && Boolean(food.id && food.name?.trim()) && positive3(food.portion_amount) && food.portion_unit !== "recipe_serving" && Object.hasOwn(foodUnitLabels, food.portion_unit) && food.exchange_system_code === EXCHANGE_SYSTEM_CODE && food.exchange_catalog_version === EXCHANGE_CATALOG_VERSION && groups2.has(food.group_code) && nutrients.every((k) => food[k] == null || typeof food[k] === "number" && Number.isFinite(food[k]) && food[k] >= 0) && !rules.excludedFoodIds?.includes(food.id) && !rules.excludedGroupCodes?.includes(food.group_code) && (rules.excludedAttributes ?? []).every((a) => food.attributes?.[a] === "free");
}
function contextRestrictions(context, rules) {
  return {
    excludedFoodIds: [.../* @__PURE__ */ new Set([...context.restrictions.excluded_food_ids, ...rules.excludedFoodIds ?? []])],
    excludedGroupCodes: [...rules.excludedGroupCodes ?? []],
    excludedAttributes: [...rules.excludedAttributes ?? []],
    likedFoodIds: [.../* @__PURE__ */ new Set([...context.preferences.catalog.filter((p) => p.preference === "like").map((p) => p.food_id), ...rules.likedFoodIds ?? []])],
    avoidedFoodIds: [.../* @__PURE__ */ new Set([...context.preferences.catalog.filter((p) => p.preference === "avoid").map((p) => p.food_id), ...rules.avoidedFoodIds ?? []])]
  };
}
function diverse(rows, maximum) {
  const unique = /* @__PURE__ */ new Map();
  for (const row of rows.sort((a, b) => b.rank - a.rank || a.key.localeCompare(b.key))) if (!unique.has(row.diversityKey)) unique.set(row.diversityKey, row);
  const buckets = /* @__PURE__ */ new Map();
  for (const row of unique.values()) {
    const key = row.exchanges.map((g) => g.group_code).sort().join("|");
    buckets.set(key, [...buckets.get(key) ?? [], row]);
  }
  const result = [];
  while (result.length < maximum && [...buckets.values()].some((b) => b.length))
    for (const bucket of buckets.values()) if (bucket.length && result.length < maximum) result.push(bucket.shift());
  return result;
}
function selectDietCandidates(context, mealTimeId, catalog, owner, explicitRules = {}, limits = DIET_GENERATION_LIMITS) {
  if (![limits.foodsPerMeal, limits.recipesPerMeal].every((n) => Number.isInteger(n) && n >= 0 && n <= 100)) throw Error("invalid_candidate_limits");
  const meal = context.meals.fact.state === "known" ? context.meals.fact.value.find((m) => m.id === mealTimeId) : void 0;
  const required = context.meal_distribution.fact.state === "known" ? context.meal_distribution.fact.value.exchanges.filter((c) => c.meal_time_id === mealTimeId && c.portions > 0) : [];
  if (context.restrictions.reaction_status.fact.state !== "known" || context.restrictions.reaction_status.fact.value !== "No" || context.restrictions.reactions.fact.state === "known" || context.restrictions.reactions.fact.state === "unavailable" && context.restrictions.reactions.fact.reason === "invalid") return { mealTimeId, candidates: [] };
  if (!meal || !required.length) return { mealTimeId, candidates: [] };
  const rules = contextRestrictions(context, explicitRules);
  const preferred = (ids) => ids.some((id) => rules.avoidedFoodIds?.includes(id)) ? -1 : ids.some((id) => rules.likedFoodIds?.includes(id)) ? 1 : 0;
  const duplicateIds = (rows) => new Set(rows.filter((r, i) => rows.findIndex((x) => x.id === r.id) !== i).map((r) => r.id));
  const duplicatedFoods = duplicateIds(catalog.foods), duplicatedRecipes = duplicateIds(catalog.recipes);
  const foods = catalog.foods.filter((f) => !duplicatedFoods.has(f.id)).map(normalizeCatalogPortion).filter((f) => hardFoodAllowed(f, owner, rules));
  const byId = new Map(foods.map((f) => [f.id, f]));
  const usable = (exchanges) => exchanges.length > 0 && exchanges.every((e) => positive3(e.portions) && required.some((r) => r.group_code === e.group_code));
  const multipliers = dietServingMultipliers();
  const foodCandidates = foods.filter((f) => required.some((r) => r.group_code === f.group_code)).map((f) => {
    const exchanges = exchangeContributionForFood(f, f.portion_amount);
    return {
      key: `food:${f.id}`,
      type: "food",
      sourceId: f.id,
      name: f.name,
      unit: f.portion_unit,
      baseQuantity: f.portion_amount,
      multipliers,
      exchanges,
      nutrition: calculateExchangeTotals(exchanges),
      food: f,
      rank: preferred([f.id]),
      diversityKey: `food:${canonicalName(f.normalized_name || f.name)}:${f.group_code}`
    };
  });
  const recipeCandidates = [];
  for (const row of catalog.recipes) {
    if (duplicatedRecipes.has(row.id) || !row.active || !(row.owner_id === null || row.owner_id === owner) || !row.id || !row.name?.trim() || !positive3(row.servings) || !row.items.length || row.meal_types.length > 0 && !row.meal_types.includes(meal.meal_type) || row.items.some((i) => !positive3(i.amount) || !byId.has(i.food_item_id ?? "") || byId.get(i.food_item_id).portion_unit !== i.unit)) continue;
    const recipe = { ...row, items: row.items.map((i) => ({ ...i, food_snapshot: createFoodSnapshot(byId.get(i.food_item_id)), exchange_contribution: exchangeContributionForFood(byId.get(i.food_item_id), i.amount) })) };
    const exchanges = recipeExchangeContributions(recipe);
    if (!usable(exchanges) || Object.values(calculateExchangeTotals(exchanges)).some((v) => !Number.isFinite(v) || v < 0)) continue;
    recipeCandidates.push({
      key: `recipe:${row.id}`,
      type: "recipe",
      sourceId: row.id,
      name: row.name,
      unit: "recipe_serving",
      baseQuantity: 1,
      multipliers,
      exchanges,
      nutrition: calculateExchangeTotals(exchanges),
      recipe,
      rank: preferred(row.items.map((i) => i.food_item_id)),
      diversityKey: `recipe:${preparationKey(recipe)}`
    });
  }
  return { mealTimeId, candidates: [...diverse(foodCandidates, limits.foodsPerMeal), ...diverse(recipeCandidates, limits.recipesPerMeal)] };
}

// frontend/src/features/diet-workshop/generationSchema.ts
var ref = { type: "string", minLength: 1, maxLength: 80 };
var dietGenerationOutputSchema = {
  type: "object",
  additionalProperties: false,
  required: ["schema_version", "meal_options"],
  properties: {
    schema_version: { type: "number", enum: [1] },
    meal_options: { type: "array", minItems: 1, maxItems: DIET_GENERATION_LIMITS.maxMeals, items: {
      type: "object",
      additionalProperties: false,
      required: ["meal_ref", "entries"],
      properties: {
        meal_ref: ref,
        entries: { type: "array", minItems: 1, maxItems: DIET_GENERATION_LIMITS.maxEntriesPerMeal, items: {
          type: "object",
          additionalProperties: false,
          required: ["candidate_ref", "portion_ref", "multiplier"],
          properties: {
            candidate_ref: ref,
            portion_ref: { type: "string", enum: ["base"] },
            multiplier: { type: "number", exclusiveMinimum: 0, maximum: DIET_GENERATION_LIMITS.maxMultiplier, multipleOf: DIET_GENERATION_LIMITS.multiplierIncrement }
          }
        } }
      }
    } }
  }
};
function parseDietModelOutput(value) {
  const issues = [];
  const fail = (path) => {
    issues.push({ code: "invalid_output", path });
  };
  const visit = (v, s, path) => {
    if ("const" in s && v !== s.const) {
      fail(path);
      return;
    }
    if (s.enum && !s.enum.includes(v)) {
      fail(path);
      return;
    }
    if (s.type === "object") {
      if (!v || typeof v !== "object" || Array.isArray(v)) {
        fail(path);
        return;
      }
      const r = v;
      if (Object.keys(r).some((k) => !Object.hasOwn(s.properties ?? {}, k)) || s.required?.some((k) => !Object.hasOwn(r, k))) {
        fail(path);
        return;
      }
      for (const [k, child] of Object.entries(s.properties ?? {})) visit(r[k], child, `${path}.${k}`);
    } else if (s.type === "array") {
      if (!Array.isArray(v) || v.length < (s.minItems ?? 0) || v.length > (s.maxItems ?? Infinity)) {
        fail(path);
        return;
      }
      v.forEach((item, i) => visit(item, s.items, `${path}[${i}]`));
    } else if (s.type === "string") {
      if (typeof v !== "string" || v.length < (s.minLength ?? 0) || v.length > (s.maxLength ?? Infinity)) fail(path);
    } else if (s.type === "number") {
      if (typeof v !== "number" || !Number.isFinite(v) || v <= (s.exclusiveMinimum ?? -Infinity) || v > (s.maximum ?? Infinity) || s.multipleOf !== void 0 && !Number.isInteger(v / s.multipleOf)) fail(path);
    }
  };
  try {
    if (JSON.stringify(value)?.length > DIET_GENERATION_LIMITS.maxResponseCharacters) return { issues: [{ code: "invalid_output", path: "$" }] };
    visit(value, dietGenerationOutputSchema, "$");
  } catch {
    fail("$");
  }
  return issues.length ? { issues } : { data: value, issues };
}

// frontend/src/features/diet-workshop/generationBoundary.ts
var defaultPolicy = { restrictions: {}, unresolved: [] };
var manualGenerationPolicy = () => structuredClone(defaultPolicy);
function baseContext(input) {
  const catalog = input.source.catalog;
  return buildDietGenerationContext({ ...input.source, catalog: catalog ? { foods: catalog.foods.slice(0, 200), recipes: catalog.recipes.slice(0, 24) } : void 0 }, input.sanitizeText);
}
function contextEligibility(input, availability) {
  const result = baseContext(input), plan = input.source.plan;
  const reasons = result.blockers.filter((b) => b !== "catalog_required").map((code) => ({ code, path: "context" }));
  const distribution = plan.meal_distribution, prescription = plan.exchange_prescription;
  const macros = result.context.prescription.macros.fact;
  const targets = macros.state === "known" ? { energy_kcal: plan.target_calories, carbohydrate_g: macros.value.CARBOHYDRATE.grams, protein_g: macros.value.PROTEIN.grams, fat_g: macros.value.FAT.grams } : null;
  if (!prescription || prescription.status !== "ready" || !prescription.confirmed_at || !targets || !sameExchangeTargets(prescription.target_snapshot, targets) || !prescription.confirmed_target_snapshot || !sameExchangeTargets(prescription.confirmed_target_snapshot, targets)) reasons.push({ code: "exchanges_unconfirmed", path: "exchange_prescription" });
  if (!distribution || distribution.status !== "ready" || !distribution.confirmed_at || !prescription || !sameExchangeInventory(distribution.source_exchange_snapshot, prescription) || !calculateDistributionStatus(distribution.distribution, prescription).canConfirm || distribution.meal_times.length > DIET_GENERATION_LIMITS.maxMeals || distribution.meal_times.some((m) => !distribution.distribution.some((c) => c.meal_time_id === m.id && c.portions > 0))) reasons.push({ code: "invalid_distribution", path: "meal_distribution" });
  if (!input.source.stamp || !Number.isSafeInteger(plan.draft_revision) || plan.draft_revision < 1) reasons.push({ code: "context_mismatch", path: "audit" });
  if (input.policy.unresolved.length) reasons.push({ code: "restrictions_need_review", path: "rules" });
  if (!input.source.catalog?.foods.length) reasons.push({ code: "catalog_required", path: "catalog" });
  if (!availability.enabled) reasons.push({ code: "feature_disabled", path: "availability" });
  if (!availability.budgetAvailable) reasons.push({ code: "insufficient_credits", path: "availability" });
  if (availability.pending) reasons.push({ code: "provider_outcome_unknown", path: "availability" });
  return { eligible: reasons.length === 0, reasons, context: result.context };
}
var fingerprint = (input) => JSON.stringify({ source: input.source, policy: input.policy });
function estimateDietPayload(payload) {
  const json = JSON.stringify(payload), schema = JSON.stringify(dietGenerationOutputSchema);
  return {
    characters: json.length,
    approximateTokens: Math.ceil(json.length / 4),
    bytes: new TextEncoder().encode(json).length,
    // Mirrors the existing provider's conservative byte bound, not billing.
    conservativeTokensWithSchema: new TextEncoder().encode(json + schema).length + 2048
  };
}
function prepareDietGeneration(input, availability, limits = DIET_GENERATION_LIMITS) {
  const eligibility = contextEligibility(input, availability);
  if (!eligibility.eligible) return { issues: eligibility.reasons };
  const context = eligibility.context, meals = input.source.plan.meal_distribution.meal_times;
  const issues = [];
  let nextCandidate = 0;
  const manifestMeals = [...meals].sort((a, b) => a.display_order - b.display_order).map((meal, i) => {
    const pool = selectDietCandidates(context, meal.id, input.source.catalog, input.source.plan.professional_id, input.policy.restrictions, limits);
    const required = input.source.plan.meal_distribution.distribution.filter((c) => c.meal_time_id === meal.id && c.portions > 0);
    if (required.some((r) => !pool.candidates.some((c) => c.exchanges.some((e) => e.group_code === r.group_code)))) issues.push({ code: "candidate_coverage_missing", path: `meals[${i}]` });
    return { ref: `m${i + 1}`, id: meal.id, candidates: pool.candidates.map((candidate) => ({ ref: `c${++nextCandidate}`, candidate })) };
  });
  if (issues.length) return { issues };
  const rules = contextRestrictions(context, input.policy.restrictions);
  const payload = {
    schema_version: 2,
    portion_policy: { portion_ref: "base", allowed_multipliers: dietServingMultipliers() },
    clinical: context.clinical,
    prescription: context.prescription,
    restrictions: { reaction_status: context.restrictions.reaction_status, reactions: context.restrictions.reactions },
    preferences: { eating_pattern: context.preferences.eating_pattern, foods: context.preferences.foods },
    routine: context.routine,
    professional_instructions: context.professional_instructions,
    rules: { hard_filtered: true, excluded_groups: rules.excludedGroupCodes ?? [], excluded_attributes: rules.excludedAttributes ?? [] },
    meals: manifestMeals.map((m) => ({
      meal_ref: m.ref,
      name: input.sanitizeText(meals.find((t) => t.id === m.id).display_name),
      distribution: input.source.plan.meal_distribution.distribution.filter((c) => c.meal_time_id === m.id && c.portions > 0).map((c) => ({ group_code: c.group_code, portions: c.portions })),
      candidates: m.candidates.map(({ ref: ref2, candidate: c }) => ({
        candidate_ref: ref2,
        type: c.type,
        name: input.sanitizeText(c.name),
        portion_ref: "base",
        unit: c.unit,
        base_quantity: c.baseQuantity,
        exchanges: c.exchanges.map((e) => ({ ...e })),
        nutrition: { ...c.nutrition }
      }))
    }))
  };
  const size = estimateDietPayload(payload);
  if (size.bytes > DIET_GENERATION_LIMITS.maxPayloadBytes) return { issues: [{ code: "input_too_large", path: "payload" }] };
  return { issues: [], prepared: { payload, size, manifest: { fingerprint: fingerprint(input), limits: { foodsPerMeal: limits.foodsPerMeal, recipesPerMeal: limits.recipesPerMeal }, meals: manifestMeals } } };
}
function getDietGenerationEligibility(input, availability) {
  const base = contextEligibility(input, availability);
  if (!base.eligible) return base;
  const prepared = prepareDietGeneration(input, availability);
  return { ...base, eligible: prepared.issues.length === 0, reasons: prepared.issues };
}
function validateDietGenerationDraft(raw, prepared, current) {
  const parsed = parseDietModelOutput(raw);
  if (!parsed.data) return { status: "invalid", issues: parsed.issues };
  if (fingerprint(current) !== prepared.manifest.fingerprint) return { status: "invalid", issues: [{ code: "context_changed", path: "manifest" }] };
  const fresh = prepareDietGeneration(current, { enabled: true, budgetAvailable: true, pending: false }, prepared.manifest.limits);
  if (!fresh.prepared) return { status: "invalid", issues: fresh.issues };
  return validateDietSnapshotDraft(raw, fresh.prepared, current.source.plan);
}
function validateDietSnapshotDraft(raw, prepared, plan) {
  const parsed = parseDietModelOutput(raw);
  if (!parsed.data) return { status: "invalid", issues: parsed.issues };
  const manifest = prepared.manifest, issues = [];
  const distribution = plan.meal_distribution;
  let draft = createDietMenu(distribution, (p) => `ai-${p}`);
  const seenMeals = /* @__PURE__ */ new Set();
  for (const [i, meal] of parsed.data.meal_options.entries()) {
    const path = `meal_options[${i}]`, allowed = manifest.meals.find((m) => m.ref === meal.meal_ref);
    if (!allowed || seenMeals.has(meal.meal_ref)) {
      issues.push({ code: "meal_not_authorized", path });
      continue;
    }
    seenMeals.add(meal.meal_ref);
    const seenCandidates = /* @__PURE__ */ new Set();
    for (const [j, entry] of meal.entries.entries()) {
      const c = allowed.candidates.find((c2) => c2.ref === entry.candidate_ref)?.candidate;
      if (!c || seenCandidates.has(entry.candidate_ref)) {
        issues.push({ code: "candidate_not_authorized", path: `${path}.entries[${j}]` });
        continue;
      }
      seenCandidates.add(entry.candidate_ref);
      if (!c.multipliers.includes(entry.multiplier)) {
        issues.push({ code: "portion_not_authorized", path: `${path}.entries[${j}]` });
        continue;
      }
      const id = `ai-entry-${i}-${j}`, quantity = c.baseQuantity * entry.multiplier;
      if (!Number.isFinite(quantity) || quantity <= 0) {
        issues.push({ code: "portion_not_authorized", path: `${path}.entries[${j}]` });
        continue;
      }
      if (c.type === "food") draft = addFoodToMenu(draft, distribution, allowed.id, c.food, quantity, id);
      else draft = addRecipeToMenu(draft, distribution, allowed.id, c.recipe, quantity, id);
    }
  }
  if (seenMeals.size !== manifest.meals.length) issues.push({ code: "meal_missing", path: "meal_options" });
  if (issues.length) return { status: "invalid", issues };
  draft.meal_options = activeMenu(draft).meal_menus.map((m, i) => ({
    id: `ai-option-${i}`,
    meal_time_id: m.meal_time_id,
    name: distribution.meal_times.find((t) => t.id === m.meal_time_id).display_name,
    entries: structuredClone(m.entries),
    status: "draft",
    confirmed_at: null,
    prescription_key: null,
    revision: 1
  }));
  draft.food_preferences = structuredClone(plan.diet_menu?.food_preferences ?? {});
  draft.status = "editing";
  draft.confirmed_at = null;
  draft.week_plan = null;
  const menuStatus = calculateMenuStatus(draft, distribution);
  const totals = calculateExchangeTotals(draft.derived_exchange_usage);
  if (Object.values(totals).some((v) => !Number.isFinite(v) || v < 0)) return { status: "invalid", issues: [{ code: "invalid_nutrition", path: "totals" }] };
  const differences = calculateExchangeDifferences(totals, plan.exchange_prescription.target_snapshot);
  return {
    status: menuStatus.canConfirm ? "valid" : "needs_adjustment",
    issues: menuStatus.rows.filter((r) => r.state !== "complete").map((r) => ({ code: "portion_difference", path: `${r.meal_time_id}.${r.group_code}` })),
    draft,
    totals,
    differences,
    // No clinical kcal/macro acceptance tolerance exists. Any difference is
    // disclosed and requires acknowledgment, not labeled clinically adequate.
    requiresTargetReview: Object.values(differences).some((v) => v !== 0)
  };
}
function hasDietMenuContent(menu) {
  return Boolean(menu && (menu.menus.some((m) => m.meal_menus.some((t) => t.entries.length)) || menu.meal_options?.length || menu.week_plan?.days.length));
}
function applyDietGenerationDraft(raw, prepared, current, decision) {
  const validation = validateDietGenerationDraft(raw, prepared, current);
  if (validation.status === "invalid") return { issues: validation.issues };
  if (hasDietMenuContent(current.source.plan.diet_menu) && !decision.replaceExisting) return { issues: [{ code: "replacement_confirmation_required", path: "diet_menu" }] };
  if ((validation.status === "needs_adjustment" || validation.requiresTargetReview) && !decision.acceptDifferences) return { issues: [{ code: "difference_confirmation_required", path: "diet_menu" }] };
  return { plan: { ...structuredClone(current.source.plan), status: "draft", diet_menu: validation.draft }, issues: [] };
}
var FakeDietGenerator = class {
  constructor(response) {
    this.response = response;
  }
  async generate(request) {
    return structuredClone(typeof this.response === "function" ? this.response(request) : this.response);
  }
};
export {
  FakeDietGenerator,
  applyDietGenerationDraft,
  dietGenerationOutputSchema,
  estimateDietPayload,
  getDietGenerationEligibility,
  hasDietMenuContent,
  manualGenerationPolicy,
  parseDietModelOutput,
  prepareDietGeneration,
  validateDietGenerationDraft,
  validateDietSnapshotDraft
};
